apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hogwarts
  name: hogwarts
spec:
  replicas: 10
  selector:
    matchLabels:
      app: hogwarts
  strategy: {}
  template:
    metadata:
      labels:
        app: hogwarts
    spec:
      containers:
        - image: httpd:2.4.65-trixie
          name: httpd
          resources: {}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge  : 1  
status: {}
